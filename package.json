{
  "name": "gaia-agents",
  "version": "1.0.0",
  "private": true,
  "description": "The official GAIA Agent Ecosystem",
  "workspaces": [
    "pkg/core",
    "agents/*"
  ],
  "scripts": {
    "test": "npm run test -w agents/*",
    "build": "npm run build -w agents/*"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "ts-node": "^10.9.1"
  }
}
